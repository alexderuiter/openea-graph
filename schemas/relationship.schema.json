{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openea.example/schema/relationship.schema.json",
  "title": "OpenEA Graph Relationship",
  "type": "object",
  "required": ["uri", "type", "from", "to"],
  "properties": {
    "uri": {
      "type": "string",
      "format": "uri",
      "description": "An absolute URI or a compact identifier using a namespace prefix declared by the repository."
    },
    "type": {
      "type": "string",
      "format": "uri",
      "description": "An absolute relationship type URI or a compact identifier using a declared namespace prefix."
    },
    "from": {
      "type": "string",
      "format": "uri",
      "description": "An absolute resource URI or a compact identifier using a declared namespace prefix."
    },
    "to": {
      "type": "string",
      "format": "uri",
      "description": "An absolute resource URI or a compact identifier using a declared namespace prefix."
    },
    "name": { "type": "string" },
    "description": { "type": "string" },
    "properties": { "type": "object", "additionalProperties": true },
    "provenance": { "$ref": "provenance.schema.json" }
  },
  "additionalProperties": false
}
